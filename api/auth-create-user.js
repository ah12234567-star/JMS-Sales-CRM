const {json,pbkdf2,salt,readBody,upsertUser}=require('./auth-utils');
module.exports=async(req,res)=>{if(req.method!=='POST')return json(res,405,{ok:false,error:'method_not_allowed'});try{const b=await readBody(req);const s=salt();const data={name:b.name||b.email,role:b.role||'rep',status:b.status||'active',password_salt:s,password_hash:pbkdf2(b.password,s)};await upsertUser({id:b.id||('u-'+Date.now()),email:String(b.email||'').toLowerCase(),phone:b.phone||'',data,updated_at:new Date().toISOString()});json(res,200,{ok:true,message:'تم إنشاء المستخدم'});}catch(e){json(res,500,{ok:false,error:e.message});}};
